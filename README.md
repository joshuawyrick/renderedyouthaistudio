# Rendered Youth AI Studio

Rebuild in progress. Preserve the original Rendered Youth logo, black/yellow/white palette, and approved brand copy.

Target: React, TypeScript, Vite, Tailwind and a server-side backend compatible with the Bolt workflow.

Printful is the selected fulfillment provider. See [integration status and workflow](docs/PRINTFUL.md). This initial foundation is not yet a runnable storefront.

Offline adapter tests (Node 24): `node --experimental-transform-types --test server/printful.test.ts`

# New TEST backend only. Never put service-role secrets in VITE_ variables.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Sample-only workflows in a hosted test build; never use for production.
VITE_WORKFLOW_PREVIEW=false
